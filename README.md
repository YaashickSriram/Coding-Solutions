# Coding Solutions
Comparison pattern - Use any of the existing data to compare and get the target. (Example : Compare the error rate of a sampler)

Else if can be used to condition 1 value to flow through the control
whereas if statement is used when conditioning applies of various values (one if statement per value)

Factorial.py - for i in range(1, num+1) - range(1, num+1) needs the +1 because range(start, stop) stops before stop — it never includes stop itself.

